2. Studio3.exe. Position 0 always contains the title of the program.

3. 
S
t
u
d
i
o
3
.
e
x
e

4. You would get an error. 
   You can't reassign a reference to a different variable (this explains the error)
   You always directly access the object with references.
   You initialize a pointer to an address but with references 
   you initialize it directly to the variable (which points to the address)

5. We saw no differences.

